---
title: 'My First Docusaurus Blog Post'
author: 'John Doe'
author_title: 'Software Engineer @ TechCorp'
author_url: 'https://github.com/johndoe'
author_image_url: 'https://avatars.githubusercontent.com/u/123456?v=4'
tags: ['docusaurus', 'mdx', 'blog']
description: 'A brief introduction to writing blogs in Docusaurus using MDX.'
image: '/img/blog-banner.png'
hide_table_of_contents: false
---

import CodeBlock from '@theme/CodeBlock';

## Welcome to My First Blog Post!

Docusaurus makes it easy to create and manage a blog with **Markdown** and **MDX**.

### Why Use MDX?

MDX allows you to embed **React components** directly inside your Markdown content.

For example, here’s how you can display a code block:

<CodeBlock language="javascript">
  {`console.log("Hello, Docusaurus!");`}
</CodeBlock>

### Adding Images

You can also include images like this:

![Docusaurus Logo](/img/docusaurus.png)

### Interactive Component

You can embed React components inside your post:

```jsx
const Counter = () => {
  const [count, setCount] = React.useState(0);
  return (
    <div>
      <p>Counter: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increase</button>
    </div>
  );
};

<Counter />;
```
